{% extends 'base.html.twig' %}

{% block title %}Pist{% endblock %}

{% block body %}
    <h1>Pist</h1>
    <audio src="{{ pist.path }}" id="audio" controls></audio>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pist.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ pist.nom }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ pist.path }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ pist.rating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pist_index') }}">back to list</a>

    <a href="{{ path('pist_edit', {'id': pist.id}) }}">edit</a>

    {{ include('pist/_delete_form.html.twig') }}
{% endblock %}
